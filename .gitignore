node_modules
.next
.turbo
dist
.env
.env.local
.env*.local
*.log
.DS_Store
coverage
.vercel
.sdd/runtime/

# Windows reserved / Bernstein
nul
NUL
.sdd/runtime/
.sdd/runs/
.sdd/audit/
.sdd/lineage/
.sdd/memory/
.sdd/catalogs/


# Next static export
apps/*/out/


# artefatos de verificação local
.playwright-mcp/
*.jpeg
ideas/

# propostas de design system geradas por app (DS-GEN) — vivem em disco p/ o gate ds-pick servir
maestro/proposals/

# scratch da skill ground-first
.ground-first/

# estado local do Maestro Control Center (operações e auditoria)
.control/

# estado ativo do profile — a fonte tracked é profiles/<slug>/profile.md
.forge/profile.md
.forge/active.txt

# apps têm repo git próprio (repo-por-app) — a fábrica não rastreia o conteúdo de apps/
apps/

# estado/arquivo de runs do maestro (a fábrica não participa dos runs)
maestro/pipelines/
maestro/runs/
maestro/lifecycle/

# prompts externalizados (>6k chars) — passados por arquivo ao CLI
maestro/.prompts/
.gstack/

# worktrees locais de implementação
.worktrees/

# runtime e dados locais do Forge Nexus
.forge-nexus/

# lixo de redirecionamento Windows e resultados de run E2E
maestro/e2e-result.md

# worktrees de agentes Claude/Codex locais
.claude/worktrees/
.codebase-memory/

